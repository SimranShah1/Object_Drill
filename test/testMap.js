const {map, cb} = require('../problem/problemMap');
const testObject = { name: 'Bruce Wayne', age: 36, location: 'Gotham' };


return map(testObject, cb);
// const result = map(testObject);
// console.log(result);